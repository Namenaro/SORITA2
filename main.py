#LUP = locally unique point

def find_some_LUP_on_pic(abs_white_point, pic):
    return LUP_name, abs_point, LUP_description

def run_description(pic, abs_point, description):
    return True

def find_rare_events_near_LUP_exemplar_on_pic(pic, LUP_name, abs_point):
    return list_of_rare_events

def try_proof_predictions_on_train_set(train_pics, LUR_name, predictions, u):
    return proved_prediction

def try_relax_description(description, abs_white_point, pic):
    return optimal_relaxed_description

def find_top_situations_with_less_relaxed_description(train_pics, LUP_description):
    return sutuations

def merge_description(descrip1, rescrip2):
    return new_rescript

def main():
    # пытаемся вырастить разные ЛУДы
    # визуализируем их
    #------------------------------------
    # ищем взаимодействия
    # растим классифицирующую структуру
